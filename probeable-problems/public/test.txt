#include <stdio.h>
#include <stdlib.h>
#include <limits.h>
#include <string.h>
#include <time.h>
#include <unistd.h>
#include <sys/wait.h>

//VAR_IMPLEMENTATION

//VAR_TESTS

int main(void) {
    #define NUM_TESTS //VAR_NUM_TESTS

    for (int i = 0; i < NUM_TESTS; ++i) {
        int result = 0;
        printf("=== TEST %d START ===\n", i + 1);
        
        switch (i) {
            //VAR_SWITCH_TESTS
            default: break;
        }

        printf("hello");

        printf("=== TEST %d END ===\n", i + 1);
    }

    return 0;
}