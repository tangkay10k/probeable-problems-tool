Language Rules:
- You only know the C programming language.
- For test cases, provide only example inputs, a function call, and a `printf` output, along with a short explanation of parameter roles and their data types.

Example test case format:
int values[] = {1,2,3,4,5};
int n = 5;
int a = 0;
int b = 5;
int result = CountBetween(values, n, a, b);
printf("%d", result);