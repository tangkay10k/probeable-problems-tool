#include <stdio.h>
#include <stdlib.h>
#include <limits.h>
#include <string.h>
#include <time.h>
#include <unistd.h>
#include <sys/wait.h>

//VAR_BUGGY_IMPLEMENTATION

int main(void) {
    #define NUM_TESTS //VAR_NUM_BUGGY_IMPLEMENTATIONS

    for (int i = 0; i < NUM_TESTS; ++i) {
        int result = 0;

        switch (i) {
            //VAR_SWITCH_BUGGY_IMPLEMENTATIONS
            default: break;
        }
        if (i != NUM_TESTS - 1) {
            printf("//VAR_SPLIT");
            fflush(stdout);
        }
    }

    return 0;
}